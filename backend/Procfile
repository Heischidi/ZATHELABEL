web: python -c "from app.database import engine, Base; from app.models import *; Base.metadata.create_all(bind=engine); print('Tables ready')" && (python seed.py 2>&1 || echo 'Seed failed, continuing...') && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}
